# solid_queue recurring task definitions. Rails 8 picks this up via
# the supervisor (bin/jobs or SOLID_QUEUE_IN_PUMA) and schedules each
# entry at the configured cadence.
#
# Note: tasks scheduled here ONLY fire when the supervisor is
# actually running. In dev, that means `bin/dev` includes a `jobs:`
# Procfile entry OR the operator sets `SOLID_QUEUE_IN_PUMA=true`
# before booting Puma. With neither, the WebUI works fine but the
# warehouse never refills.
#
# solid_queue reads the env-matching section (Rails.env). The
# `common` anchor at the top lets us share entries across dev/prod
# without copy-paste — production adds the housekeeping job on top.
# Test env is intentionally absent so the spec suite doesn't fight
# with a background ticker.

common: &common
  metrics_sync:
    class: MetricsSyncOrchestratorJob
    # 14s sync paired with MetricsData.CACHE_TTL=10s — the 4s gap
    # guarantees that whatever cache window was warmed at sync T
    # has expired by sync T+1, so each broadcast-triggered
    # frame.reload() always cache-misses and pulls fresh data.
    # Sits just under the 15s controller-side sampler cadence
    # so we don't accidentally lap it. Cheap NDJSON-tail-and-
    # bulk-insert; cadence cost is nothing on local SQLite.
    schedule: every 14 seconds

  state_sync:
    class: StateSyncOrchestratorJob
    # 15s cadence. Each tick pulls /pods?detail=true&spec=true
    # which on the controller side now uses the docker SDK (no
    # more fork+exec per stat call) — the historic 100% CPU
    # spikes that forced us to widen this to 30s are gone. Back
    # to a tighter window so the pods table CPU/Mem chips +
    # status pills feel responsive again.
    #
    # If the controller's CPU starts spiking again (e.g. after
    # adding many new chatty pods), the cheapest knob is to
    # raise this back to 30s; deeper levers live in the
    # controller's VOODU_METRICS_INTERVAL + VOODU_STATS_STAGGER_MS.
    #
    # Independent of metrics-sync (different data path; metrics-
    # sync pulls pre-sampled NDJSON from /metrics/dump, doesn't
    # invoke docker stats).
    schedule: every 15 seconds

  log_metrics_sync:
    class: LogMetricsSyncOrchestratorJob
    # 15s — matches the log-tail poll cadence (no point counting faster than
    # logs land). Each per-server tick recomputes only the live 10-min window
    # (cheap) + a one-time deep backfill per newly-pinned filter. Reads the
    # local NDJSON warehouse, so it runs in BOTH Ruby-tail and Go-poller modes
    # (no POLLER_SPAWN guard) — it's the only thing turning logs into counts
    # when the binary owns tailing.
    schedule: every 15 seconds

  alerts_evaluation:
    class: AlertsEvaluationOrchestratorJob
    # 30s — two warehouse sync ticks (14s) land between evaluations,
    # so each pass sees fresh buckets without re-judging the same
    # data. Local SQLite reads only; detection latency ceiling is
    # rule duration + ~45s, fine for infra alerting.
    schedule: every 30 seconds

  hep3_sync:
    class: Hep3SyncOrchestratorJob
    # 15s — matches the collector's write cadence. Each tick drains the
    # /export NDJSON tail of every configured voodu-hep3 reader into the
    # `hep` read model. No-op for servers without the plugin installed or
    # with no reader instance configured.
    schedule: every 15 seconds

  log_tail_orchestrator:
    class: LogTailOrchestratorJob
    # 1 min cadence — fast enough to recover from a crashed tail job
    # in under a minute, slow enough that the per-server TailLock
    # check overhead is negligible. Per-server work is dropped at
    # this tick if a tail is already in flight (TailLock guard).
    schedule: every 1 minute

  log_tail_cleanup:
    class: LogTailCleanupJob
    # 03:00 daily — low-traffic window. Reaps NDJSON files older
    # than RETENTION_DAYS (2). Empty pod-dirs get removed too so
    # decommissioned pods don't leak the tree.
    schedule: every day at 3:00 am

development:
  <<: *common

production:
  <<: *common
  # solid_queue grows finished_jobs unbounded by default. The hourly
  # batched delete keeps the queue DB lean without spiking IO
  # (sleep_between_batches throttles).
  clear_solid_queue_finished_jobs:
    command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
    schedule: every hour at minute 12
